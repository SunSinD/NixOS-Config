# NixOS Config

## Install

Connect to Wi-Fi:

```bash
nmtui
```

Choose `Activate a connection`, select your Wi-Fi network, then enter the password.

Run the installer:

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/SunSinD/NixOS-Config/main/install.sh)
```
